# 복사: copy .env.example .env  (또는 수동으로 server/.env 생성)
# 웹(UI) + API + Socket.IO 동일 PORT
#
# 수위 PCB: 대문자 R 송신 → 27바이트 ASCII (r, + 센서20 + , + mm 4자리)
# WATER_LEVEL_POLL_MS: R 재전송 주기(ms), 최소 100

SERIAL_PORT=COM1
BAUD_RATE=9600
WATER_LEVEL_POLL_MS=500
PORT=8000